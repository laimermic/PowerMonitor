export class FrequencyHour {
    public hour: number;
    public min: number;
    public max: number;

    constructor(hour: number, min: number, max: number) {
        this.hour = hour;
        this.min = min;
        this.max = max;
    }
}