import { FindPricePipe } from './find-price.pipe';

describe('FindPricePipe', () => {
  it('create an instance', () => {
    const pipe = new FindPricePipe();
    expect(pipe).toBeTruthy();
  });
});
